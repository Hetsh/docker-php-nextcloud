[Unit]
Description=PHP FastCGI Process Manager for Nextcloud
After=docker.service
Requires=docker.service

[Service]
ExecStartPre=-/bin/docker create \
              --rm \
              --publish 9000:9000/tcp \
              --mount type=bind,source="/srv",target="/srv" \
              --mount type=bind,source=/etc/localtime,target=/etc/localtime,readonly \
              --name php-fpm-cloud \
              hetsh/php-fpm-nextcloud
ExecStart=/bin/docker start --attach php-fpm-cloud
ExecStop=/bin/docker stop php-fpm-cloud

[Install]
WantedBy=multi-user.target
